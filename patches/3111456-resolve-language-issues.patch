diff --git a/phpstan-baseline.neon b/phpstan-baseline.neon
index 310ea7a..25193b6 100644
--- a/phpstan-baseline.neon
+++ b/phpstan-baseline.neon
@@ -5,3 +5,8 @@ parameters:
 			identifier: new.static
 			count: 1
 			path: src/Controller/PathTranslator.php
+		-
+		  message: '#^Call to an undefined method Drupal\\language\\LanguageNegotiationMethodInterface::processInbound\(\).$#'
+		  identifier: method.notFound
+		  count: 1
+		  path: src/EventSubscriber/RouterPathTranslatorSubscriber.php
diff --git a/src/EventSubscriber/RedirectPathTranslatorSubscriber.php b/src/EventSubscriber/RedirectPathTranslatorSubscriber.php
index 557fc51..b2f214a 100644
--- a/src/EventSubscriber/RedirectPathTranslatorSubscriber.php
+++ b/src/EventSubscriber/RedirectPathTranslatorSubscriber.php
@@ -24,6 +24,26 @@ use Symfony\Component\Routing\Matcher\UrlMatcherInterface;
  */
 class RedirectPathTranslatorSubscriber extends RouterPathTranslatorSubscriber {
 
+  /**
+   * Constructs a RedirectPathTranslatorSubscriber object.
+   *
+   * @param \Symfony\Component\DependencyInjection\ContainerInterface $container
+   *   The service container.
+   * @param \Psr\Log\LoggerInterface $logger
+   *   The logger channel.
+   * @param \Symfony\Component\Routing\Matcher\UrlMatcherInterface $router
+   *   The router.
+   * @param \Drupal\Core\Extension\ModuleHandlerInterface $module_handler
+   *   The module handler.
+   * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
+   *   The config factory.
+   * @param \Drupal\path_alias\AliasManagerInterface $aliasManager
+   *   The alias manager.
+   * @param \Drupal\Core\Language\LanguageManagerInterface $languageManager
+   *   The language manager.
+   * @param \Drupal\redirect\RedirectRepository $redirectRepository
+   *   The redirect repository.
+   */
   public function __construct(
     ContainerInterface $container,
     #[Autowire(service: 'logger.channel.decoupled_router')] LoggerInterface $logger,
@@ -68,21 +88,51 @@ class RedirectPathTranslatorSubscriber extends RouterPathTranslatorSubscriber {
     $request_query = $original_parsed_url['query'];
     $source_path = $this->cleanSubdirInPath($original_parsed_url['path'], $event->getRequest());
 
+    // Get system path from alias and handle language prefix.
+    if ($this->container->get('language_manager')->isMultilingual()) {
+      $source_path = $this->getPathFromAlias($source_path);
+    }
+
+    // Now remove the language prefix for redirect lookup.
+    $source_path = $this->removeLanguagePrefix($source_path);
+
     $cacheable_metadata = new CacheableMetadata();
-    $cacheable_metadata->addCacheableDependency($this->configFactory->get('redirect.settings'));
+    $cacheable_metadata->addCacheableDependency(
+      $this->configFactory->get('redirect.settings')
+    );
 
+    // Try to find redirect with decoded path first.
+    // This handles encoded URLs like Arabic characters.
+    $decoded_source_path = urldecode($source_path);
     $redirect = $this->redirectRepository->findMatchingRedirect(
-      $source_path,
+      $decoded_source_path,
       $request_query,
-      $this->languageManager->getCurrentLanguage()->getId(),
+      $this->langcode ?? $this->languageManager->getCurrentLanguage()->getId(),
       $cacheable_metadata
     );
+
+    // If not found with decoded path, try with original encoded path.
+    if (!$redirect) {
+      $redirect = $this->redirectRepository->findMatchingRedirect(
+        $source_path,
+        $request_query,
+        $this->langcode ?? $this->languageManager->getCurrentLanguage()->getId(),
+        $cacheable_metadata
+      );
+    }
+
     if (!$redirect) {
       return;
     }
 
     $response->addCacheableDependency($cacheable_metadata);
     $redirect_url = $redirect->getRedirectUrl();
+
+    // Ensure the redirect URL maintains the correct language.
+    if ($this->langcode) {
+      $redirect_url->setOption('language', $this->languageManager->getLanguage($this->langcode));
+    }
+
     if ($this->configFactory->get('redirect.settings')->get('passthrough_querystring')) {
       $redirect_url->setOption('query', (array) $redirect_url->getOption('query') + $request_query);
     }
@@ -138,4 +188,25 @@ class RedirectPathTranslatorSubscriber extends RouterPathTranslatorSubscriber {
     $event->stopPropagation();
   }
 
+  /**
+   * Removes the language prefix from a path if present.
+   *
+   * @param string $path
+   *   The path that may contain a language prefix.
+   *
+   * @return string
+   *   The path without the language prefix.
+   */
+  protected function removeLanguagePrefix($path) {
+    // Get available language prefixes.
+    $config = $this->configFactory->get('language.negotiation')->get('url');
+    if (empty($config['prefixes'])) {
+      return $path;
+    }
+
+    // Remove the leading slash for matching.
+    $path = ltrim($path, '/');
+    return preg_replace('/^(' . implode('|', array_values($config['prefixes'])) . ')\//i', '', $path);
+  }
+
 }
diff --git a/src/EventSubscriber/RouterPathTranslatorSubscriber.php b/src/EventSubscriber/RouterPathTranslatorSubscriber.php
index 11c5492..fe0eb64 100644
--- a/src/EventSubscriber/RouterPathTranslatorSubscriber.php
+++ b/src/EventSubscriber/RouterPathTranslatorSubscriber.php
@@ -10,6 +10,7 @@ use Drupal\Core\Config\ConfigFactoryInterface;
 use Drupal\Core\Entity\ContentEntityType;
 use Drupal\Core\Entity\EntityInterface;
 use Drupal\Core\Entity\EntityMalformedException;
+use Drupal\Core\Entity\TranslatableInterface;
 use Drupal\Core\Extension\ModuleHandlerInterface;
 use Drupal\Core\Routing\RouteObjectInterface;
 use Drupal\Core\StringTranslation\StringTranslationTrait;
@@ -84,6 +85,13 @@ class RouterPathTranslatorSubscriber implements EventSubscriberInterface {
    */
   protected const LOG_ENTITY_NOT_FOUND = TRUE;
 
+  /**
+   * The langcode if added as a prefix to the path.
+   *
+   * @var string
+   */
+  protected $langcode;
+
   /**
    * RouterPathTranslatorSubscriber constructor.
    *
@@ -129,6 +137,11 @@ class RouterPathTranslatorSubscriber implements EventSubscriberInterface {
     }
     $path = $this->cleanSubdirInPath($event->getPath(), $event->getRequest());
 
+    // Get system path from alias if applicable.
+    if ($this->container->get('language_manager')->isMultilingual()) {
+      $path = $this->getPathFromAlias($path);
+    }
+
     // Preserve the original query string and fragment if any.
     $resolved_url_options = UrlHelper::parse($path);
     unset($resolved_url_options['path']);
@@ -183,6 +196,17 @@ class RouterPathTranslatorSubscriber implements EventSubscriberInterface {
 
     $entity_type_id = $entity->getEntityTypeId();
 
+    // Get entity translation if applicable.
+    if (!empty($this->langcode)) {
+      if ($entity instanceof TranslatableInterface && $entity->hasTranslation($this->langcode)) {
+        $entity = $entity->getTranslation($this->langcode);
+      }
+      else {
+        $entity = $this->container->get('entity.repository')->getTranslationFromContext($entity, $this->langcode);
+      }
+    }
+    $resolved_url_options['language'] = $entity->language();
+
     try {
       $canonical_url = $entity->toUrl('canonical', ['absolute' => TRUE])->toString(TRUE);
     }
@@ -221,6 +245,12 @@ class RouterPathTranslatorSubscriber implements EventSubscriberInterface {
         'uuid' => $entity->uuid(),
       ],
     ];
+
+    // Only add langcode if available.
+    if ($entity instanceof TranslatableInterface) {
+      $output['langcode'] = $entity->language()->getId();
+    }
+
     if ($label_accessible->isAllowed()) {
       $output['label'] = $entity->label();
     }
@@ -236,23 +266,37 @@ class RouterPathTranslatorSubscriber implements EventSubscriberInterface {
       $rt_repo = $this->container->get('jsonapi.resource_type.repository');
       $rt = $rt_repo->get($entity_type_id, $entity->bundle());
       $type_name = $rt->getTypeName();
-      $jsonapi_base_path = $this->container->getParameter('jsonapi.base_path');
-      $entry_point_url = Url::fromRoute('jsonapi.resource_list', [], ['absolute' => TRUE])->toString(TRUE);
+      $jsonapi_base_path = Url::fromRoute(
+        'jsonapi.resource_list',
+        [],
+        ['language' => $entity->language()]
+      )->toString(TRUE);
+      $entry_point_url = Url::fromRoute(
+        'jsonapi.resource_list',
+        [],
+        [
+          'absolute' => TRUE,
+          'language' => $entity->language(),
+        ]
+      )->toString(TRUE);
       $route_name = sprintf('jsonapi.%s.individual', $type_name);
       $individual = Url::fromRoute(
         $route_name,
         [
           static::getEntityRouteParameterName($route_name, $entity_type_id) => $entity->uuid(),
         ],
-        ['absolute' => TRUE]
+        [
+          'absolute' => TRUE,
+          'language' => $entity->language(),
+        ]
       )->toString(TRUE);
       $response->addCacheableDependency($entry_point_url);
       $response->addCacheableDependency($individual);
       $output['jsonapi'] = [
         'individual' => $individual->getGeneratedUrl(),
         'resourceName' => $type_name,
-        'pathPrefix' => trim($jsonapi_base_path, '/'),
-        'basePath' => $jsonapi_base_path,
+        'pathPrefix' => trim($jsonapi_base_path->getGeneratedUrl(), '/'),
+        'basePath' => $jsonapi_base_path->getGeneratedUrl(),
         'entryPoint' => $entry_point_url->getGeneratedUrl(),
       ];
       $output['meta'] = [
@@ -429,4 +473,36 @@ class RouterPathTranslatorSubscriber implements EventSubscriberInterface {
     return $resolved_url === $home_url;
   }
 
+  /**
+   * Convert an alias to its source path.
+   *
+   * This is a workaround for a bug where matcher fails on aliases prefixed by
+   * a language prefix when it doesn't match the negotiated language.
+   *
+   * @param string $path
+   *   The input path string.
+   *
+   * @return string
+   *   The output path string.
+   */
+  protected function getPathFromAlias(string $path): string {
+    // Decode the path to handle encoded characters (e.g., Arabic).
+    $decoded_path = urldecode($path);
+
+    $config = $this->configFactory->get('language.negotiation')->get('url');
+    $language_negotiation_url = $this->container->get('language_negotiator')
+      ->getNegotiationMethodInstance('language-url');
+    $router_request = Request::create($decoded_path);
+    $langcode = $language_negotiation_url->getLangcode($router_request);
+    $prefix = $config['prefixes'][$langcode] ?? NULL;
+    if ($prefix && ($decoded_path == "/$prefix" || str_starts_with($decoded_path, "/$prefix/"))) {
+      $this->langcode = $langcode;
+      $path_without_prefix = $language_negotiation_url->processInbound($decoded_path, $router_request);
+      $decoded_path = $this->aliasManager->getPathByAlias($path_without_prefix, $langcode);
+      $decoded_path = "/$prefix" . $decoded_path;
+    }
+
+    return $decoded_path;
+  }
+
 }
diff --git a/tests/src/Functional/DecoupledRouterFunctionalTest.php b/tests/src/Functional/DecoupledRouterFunctionalTest.php
index 0d10a64..e23298d 100644
--- a/tests/src/Functional/DecoupledRouterFunctionalTest.php
+++ b/tests/src/Functional/DecoupledRouterFunctionalTest.php
@@ -311,12 +311,13 @@ class DecoupledRouterFunctionalTest extends BrowserTestBase {
         'id' => $node->id(),
         'uuid' => $node->uuid(),
       ],
+      'langcode' => 'en',
       'label' => $node->label(),
       'jsonapi' => [
         'individual' => $this->buildUrl('/jsonapi/node/article/' . $node->uuid()),
         'resourceName' => 'node--article',
-        'pathPrefix' => 'jsonapi',
-        'basePath' => '/jsonapi',
+        'pathPrefix' => 'web/jsonapi',
+        'basePath' => '/web/jsonapi',
         'entryPoint' => $this->buildUrl('/jsonapi'),
       ],
       'meta' => [
@@ -424,12 +425,13 @@ class DecoupledRouterFunctionalTest extends BrowserTestBase {
         'id' => $node->id(),
         'uuid' => $node->uuid(),
       ],
+      'langcode' => 'en',
       'label' => $node->label(),
       'jsonapi' => [
         'individual' => $this->buildUrl('/jsonapi/node/article/' . $node->uuid()),
         'resourceName' => 'node--article',
-        'pathPrefix' => 'jsonapi',
-        'basePath' => '/jsonapi',
+        'pathPrefix' => 'web/jsonapi',
+        'basePath' => '/web/jsonapi',
         'entryPoint' => $this->buildUrl('/jsonapi'),
       ],
       'meta' => [
@@ -539,12 +541,13 @@ class DecoupledRouterFunctionalTest extends BrowserTestBase {
         'id' => $node->id(),
         'uuid' => $node->uuid(),
       ],
+      'langcode' => 'en',
       'label' => $node->label(),
       'jsonapi' => [
         'individual' => $this->buildUrl('/jsonapi/node/article/' . $node->uuid()),
         'resourceName' => 'node--article',
-        'pathPrefix' => 'jsonapi',
-        'basePath' => '/jsonapi',
+        'pathPrefix' => 'web/jsonapi',
+        'basePath' => '/web/jsonapi',
         'entryPoint' => $this->buildUrl('/jsonapi'),
       ],
       'meta' => [
diff --git a/tests/src/Functional/DecoupledRouterInfoAlterTest.php b/tests/src/Functional/DecoupledRouterInfoAlterTest.php
index 2157824..e2c9648 100644
--- a/tests/src/Functional/DecoupledRouterInfoAlterTest.php
+++ b/tests/src/Functional/DecoupledRouterInfoAlterTest.php
@@ -95,8 +95,8 @@ class DecoupledRouterInfoAlterTest extends BrowserTestBase {
       'jsonapi' => [
         'individual' => $this->buildUrl('/jsonapi/node/article/' . $node->uuid()),
         'resourceName' => 'node--article',
-        'pathPrefix' => 'jsonapi',
-        'basePath' => '/jsonapi',
+        'pathPrefix' => 'web/jsonapi',
+        'basePath' => '/web/jsonapi',
         'entryPoint' => $this->buildUrl('/jsonapi'),
       ],
       'meta' => [
@@ -105,6 +105,7 @@ class DecoupledRouterInfoAlterTest extends BrowserTestBase {
         ],
       ],
       'isExternal' => FALSE,
+      'langcode' => 'en',
     ];
     $this->assertEquals($expected, $output);
 
