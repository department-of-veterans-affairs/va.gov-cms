langcode: en
status: true
dependencies:
 module:
   - va_gov_scheduled_tasks
   - va_gov_migrate
title: 'Archive IntranetOnly forms'
description: 'This is because we cannot access the server on the SOCKS proxy right now.'
id: archive_intranet_only_forms
module: va_gov_scheduled_tasks
callback: va_gov_scheduled_tasks_archive_intranet_only_forms_cron
scheduler:
 id: simple
 configuration:
   rules:
     - '0+@ 0 * * *'