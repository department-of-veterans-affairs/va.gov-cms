langcode: en
status: true
dependencies:
 module:
   - va_gov_scheduled_tasks
title: 'Do scheduled tasks every minute'
description: 'Content release and metrics'
id: va_gov_scheduled_tasks_every_minute
weight: 0
module: va_gov_scheduled_tasks
callback: va_gov_scheduled_tasks_every_minute
scheduler:
 id: crontab
 configuration:
   rules:
     - '* * * * *'