# https://docs.lagoon.sh/using-lagoon-the-basics/lagoon-yml/
docker-compose-yaml: docker-compose.yml

project: demo-vagov-cmstest

tasks:
#  # pre-rollout tasks run on the old containers
#  pre-rollout:

  # post-rollout tasks run on the new containers
  post-rollout:
    - run:
        name: Copy Lagoon .env into place
        command: ep .lagoon/.env.conf .env
        service: cli
        shell: bash
#    - run:
#        name: Run drush deploy
#        command: drush deploy
#        service: cli
#        shell: bash
