version: '3'
dotenv: [ '.env' ]
output: 'group'

tasks:

  build-frontend:
    desc: Request a frontend build
    cmds:
    - drush va-gov-content-release:request:submit

  build:
    deps:
    - build-frontend

  default:
    cmds:
    - task: build
